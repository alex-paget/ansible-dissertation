# User/dynamic Variables

# External IP
sms_ip: "{{ ansible_ens37.ipv4.address }}"
sms_name: "{{ ansible_hostname }}"
sms_eth_internal: "{{ ansible_ens37.device }}"
sms_netmask: "{{ ansible_ens37.ipv4.netmask }}"
sms_network: "{{ ansible_ens37.ipv4.network }}"
